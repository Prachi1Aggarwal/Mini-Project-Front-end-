git status => to check git status

git init => a folder will be created with the name .git

git add . or <file name> 

git push => 

git commit -m <message> {before this user authentication should be there for that we need (git config --global user.email "<emailid>")}